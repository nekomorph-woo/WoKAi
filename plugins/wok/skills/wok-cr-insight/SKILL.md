---
name: wok-cr-insight
description: >
  分析审查报告中的 🟡 Advisory 问题，在每个问题下方追加根因追溯、修改方案和一致性评估。
  Use when 用户要求分析 advisory 问题、深度分析审查报告、提到
  "wok-cr-insight" / "cr-insight" / "洞察"。
pipeline:
  upstream: [wok-code-review]
  downstream: []
  gate: false
  output: _review.md（追加）
  adaptive: false
---

# 🟡 问题深度分析

读取 `_review.md` 中的 🟡 Advisory 问题，追加原因分析、修改方案和一致性评估。

## 命令接口

```
/wok-cr-insight [--file <path>]
```

| 参数 | 格式 | 默认值 | 说明 |
|------|------|--------|------|
| `--file` | 文件路径 | 自动定位 | 指定 `_review.md` 路径 |

## 执行流程

### Step 1: 定位输入

1. 若指定 `--file`，直接使用该路径
2. 否则搜索 `plans/` 目录下的 `_review.md`（与 review-engine Stage 0 检测逻辑一致）
3. 验证文件存在
4. 提取文件中的 🟡 问题数量，若为 0 → 输出"无 Advisory 问题需要分析"并退出
5. **检测管道上下文**：搜索 `plans/` 目录下的 `_define.md`（与 review-engine 使用相同的检测路径）

### Step 2: 解析 🟡 问题

1. 提取所有 🟡 级别问题条目
2. 跳过已有 `🔍 原因分析` 区块的问题（**幂等**）
3. 若全部已有分析 → 输出"所有 Advisory 问题已有分析"并退出

### Step 3: 逐个分析

对每个未分析的问题：

1. **读取源代码**：读取问题涉及的源代码上下文（Read 工具）
2. **根因追溯**：分类为以下之一
   - 设计缺陷：需求阶段遗漏或设计不充分
   - 编码疏忽：实现过程中的疏忽或简化
   - 架构约束：现有架构导致的最优妥协
   - 上下文缺失：缺少领域知识或业务背景
3. **修改方案**：给出可直接执行的代码，DO NOT 给出伪代码
4. **一致性评估**（仅管道模式）：对照 `_define.md` 设计锚点评估

### Step 4: 写入追加

在每个 🟡 问题下方追加 3 个引用块：

```markdown
> **🔍 原因分析**
> <根因分类>: <详细说明>

> **🔧 修改方案**
> <具体修改步骤，含完整代码>

> **📐 一致性评估**
> <与 PRD/设计目标的关系评估>
```

无管道上下文时，一致性评估替换为：

```markdown
> **📐 一致性评估**
> ⚠️ 无管道上下文，跳过一致性评估
```

写回 `_review.md`。

## 实现约束

- **幂等性**：重复调用不重复追加，通过检测 `🔍 原因分析` 区块判断
- **只读源码**：仅读取源代码，DO NOT 修改源代码文件
- **只写报告**：仅修改 `_review.md`
- **管道模式优先**：有 PRD 上下文时必须执行一致性评估
- **独立模式降级**：无管道上下文时标注跳过
- **分析触及根因**：DO NOT 仅重述问题现象
- **代码示例完整**：DO NOT 给出伪代码
